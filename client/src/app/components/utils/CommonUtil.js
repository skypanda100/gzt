/**
 * Created by Administrator on 2017/6/10.
 */
class CommonUtil {
    constructor() {
    }

    static trim(str) {
        if(str.isPrototypeOf()){

        }
        return str.replace(/^\s\s*/, '').replace(/\s\s*$/, '');
    }
}

export default CommonUtil;
